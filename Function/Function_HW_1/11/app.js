// Задача 14 
// На входе массив. Необходимо создать функцию, возвращающую количество элементов массива

const arr = [1,2,3,4,5,5,6,6,7,8]

let finedLenghtArray = (el) => {
    return el.length 
}
console.log(finedLenghtArray(arr));